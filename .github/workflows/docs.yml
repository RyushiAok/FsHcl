name: docs

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: docs/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: docs
        run: pnpm install --frozen-lockfile
      - name: Build docs
        working-directory: docs
        run: pnpm build
      - name: Prepare deploy
        working-directory: docs/build/client
        run: |
          cp -r assets FsHcl/
          cp __spa-fallback.html FsHcl/404.html
          cp robots.txt FsHcl/
          cp google338969153909947f.html FsHcl/
      - uses: actions/upload-pages-artifact@v4
        with:
          path: docs/build/client/FsHcl

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
